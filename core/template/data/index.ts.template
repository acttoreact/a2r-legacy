import api from 'a2r/api';
import { GetPageData } from '../config/data';
import { PageData } from '../pages/index';

const getData: GetPageData<PageData> = async () => {
  const data = await api.getData();
  return {...data};
};

export default getData;
