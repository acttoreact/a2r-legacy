import * as model from 'a2r/model';

/**
 * Gets data from server
 * @returns {Promise<model.Data>}
 */
const getData = async (): Promise<model.Data> => {
  console.log('data server method!');
  return {
    info: 'Data from API',
  };
};

export default getData;
